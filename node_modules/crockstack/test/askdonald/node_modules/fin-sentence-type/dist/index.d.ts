import * as Fin from "finnlp";
declare module "finnlp" {
    interface SentenceTypeObject {
        type: string;
        confidence: number;
    }
    interface Run {
        sentenceType: (this: Fin.Run) => SentenceTypeObject[][];
    }
}
